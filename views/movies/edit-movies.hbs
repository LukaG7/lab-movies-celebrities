<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Edit movie</h1>

<form action="/movies/{{movieData._id}}/edit" method="POST"> 
 <label>Title</label>
        <input type="text" name="title" placeholder="{{movieData.title}}">
    <label>Genre</label>
        <input type="text" name="genre" placeholder="{{movieData.genre}}">
    <label>Plot</label>
        <input type="" name="plot" placeholder="{{movieData.plot}}">
    <label>Cast</label>
        <select name="cast" multiple>
            {{#each celebData}}
                <option value="{{this._id}}">{{this.name}}</option>
            {{/each}}
        </select>
    <button type="submit">Edit</button>
</form>
</body>
</html>